paths:
  static: public
  uploads: public/uploads

timezone: Asia/Tokyo

markdown:
  engine: commonmark
  options:
    gfm: true

collections_config:
  pages:
    path: src/pages
    icon: wysiwyg

  blog:
    path: src/content/blog
    icon: event_available
